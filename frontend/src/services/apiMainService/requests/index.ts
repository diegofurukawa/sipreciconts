// src/services/apiMainService/requests/index.ts
import { baseRequests } from './baseRequests';
import { paginatedRequests } from './paginatedRequests';
import { fileRequests } from './fileRequests';

export {
  baseRequests,
  paginatedRequests,
  fileRequests
};

export default {
  baseRequests,
  paginatedRequests,
  fileRequests
};